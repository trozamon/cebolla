= form_with scope: :line_item,
  url: invoice_line_items_path(@invoice),
  local: true,
  class: 'ui form' do |f|

  = render partial: 'fields', locals: { f: f }
  = f.submit 'Add', class: 'ui button'

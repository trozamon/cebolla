%h1 Add an entity

= form_with model: @entity do |f|
  = f.label :name
  = f.text_field :name

  = f.label :email
  = f.text_field :email

  = f.label :phone
  = f.text_field :phone

  = f.fields_for :address do |addr_form|
    = render partial: 'shared/address/fields', locals: { f: addr_form }

  = f.submit

.ui.breadcrumb
  = link_to 'Dashboard', root_path, class: 'section'
  %i.right.angle.divider.icon
  = link_to 'Projects', projects_path, class: 'section'
  %i.right.angle.divider.icon
  .active.section New Project

%p Get that cheddar!

= form_with scope: :project, url: projects_path, class: 'ui form' do |f|
  = render partial: 'fields', locals: { f: f }
  = f.submit 'Create Project', class: 'ui button'

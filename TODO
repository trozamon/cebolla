write a functional spec

other desired features:
* make credits any time that are picked up by the invoicing process
* archive projects
* calendar of hours worked
* kanban board thingy across all projects
* breadcrumbs

reminders for generating invoices
  e.g. for a monthly account that bills on the 1st
  if current month has no invoice, show a warning + link to make invoice
